function printOrder(orderId, buyer, total, status) {
  const printWindow = window.open('', '', 'width=800,height=600');

  const htmlContent = `
    <!DOCTYPE html>
    <html>
    <head>
      <title>Print Order - ${orderId}</title>
      <style>
        body {
          font-family: Arial, sans-serif;
          padding: 40px;
          line-height: 1.6;
        }
        h1 {
          color: #ea580c;
        }
        table {
          width: 100%;
          border-collapse: collapse;
          margin-top: 20px;
        }
        td, th {
          border: 1px solid #ddd;
          padding: 12px;
        }
        th {
          background-color: #f3f4f6;
        }
        .text-right { text-align: right; }
        .text-center { text-align: center; }
        footer {
          margin-top: 60px;
          font-size: 0.9rem;
          text-align: center;
        }
      </style>
    </head>
    <body>
      <h1>Order Summary</h1>
      <table>
        <tr><th>Order ID</th><td>${orderId}</td></tr>
        <tr><th>Buyer</th><td>${buyer}</td></tr>
        <tr><th>Total</th><td class="text-right">₱${total.toFixed(2)}</td></tr>
        <tr><th>Status</th><td class="text-center">${status}</td></tr>
      </table>

      <footer>
        <p>Generated by OrderByte - Shopee ISV Demo</p>
        <p>${new Date().toLocaleString()}</p>
      </footer>

      <script>
        window.onload = function () {
          window.print();
        };
      </script>
    </body>
    </html>
  `;

  printWindow.document.write(htmlContent);
  printWindow.document.close();
}
